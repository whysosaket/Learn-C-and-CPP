#include<stdio.h>

int main(){

    // variable  = allocated space in memory to store a value
    // this is very similar to java

    int x = 5;  // declaration and initialization
    char name []= "Saket Aryan"; // This is an array of characters of string
    printf("%d %s",x,"\n");
    printf("This is the second line");

    return 0;
}