#include <stdio.h>

int main(){
    printf("This is the code that will be run using the terminal!\n");
    return 0;
}

// we can use the comand gcc "filename.c" to compile the C file
// not a file will be created and we can run this file using the terminal itself
// command will be ./filename
